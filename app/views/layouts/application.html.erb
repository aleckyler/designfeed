<!DOCTYPE html>
<html>
<head>
  <title>DesignFeed</title>
  <link href='//fonts.googleapis.com/css?family=Unica+One' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <p class="alert"><%= alert %></p>
  <% if user_signed_in? %>
    <% if Profile.where(user_id: current_user.id).length != 0 %>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %> |
      <%= link_to "View My Profile", profile_path(Profile.where(user_id: current_user.id).first.id) %> |
      <%= link_to "Edit My Profile", edit_profile_path(Profile.where(user_id: current_user.id).first.id) %>
    <% end %>
  <%else%>
    <%= link_to "Sign Up", new_user_registration_path %> |
    <%= link_to "Sign In", new_user_session_path %>
  <% end %>
<%= yield %>

</body>
</html>
