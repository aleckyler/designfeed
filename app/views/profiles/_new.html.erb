<% if Profile.where(user_id: current_user.id).length != 0 %>
  <h2>You already have a profile.</h2>
  <h2>You can't make another.</h2>
<% else %>
  <h1>New Profile</h1>

  <%= render 'profiles/form' %>

  <%= link_to 'Back', :back %>
<% end %>
